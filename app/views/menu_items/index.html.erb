
<h2>Menu</h2>
<div class="menu-items">
  <% @menu_items.each do |item| %>
    <div class="menu-item">
      <%= image_tag(item.image) %>
      <h3><%= item.name %></h3>
      <p><%= item.description %></p>
      <p>$<%= item.price %></p>
    </div>
  <% end %>
</div>
